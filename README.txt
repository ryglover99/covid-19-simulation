The matricies file contains two matrix's that can be used in the simulation. 

The simulation currently has the office layout matrix implemented. 

To change the matrix to the clear one, that is also inside the matrix file, just copy and paste it over the matrix in the simulation file and run.